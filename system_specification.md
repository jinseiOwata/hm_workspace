# システム仕様書: LINE分析→楽天証券RSS IFD/IFO当日発注

実装は自宅VSCode側で行う。ここでは仕様の確定のみ行う。

## 全体アーキテクチャ

```
[朝の起動] (Task Scheduler等)
   ↓
① 分析生成 (Claude API, JSON付き出力)
   ↓
② 解析 (line_analysis_parser.py) → シグナルリスト
   ↓
③ 保存+通し番号付与 (line_selection.save_today_signals)
   ↓
④ LINEへpush送信 (line_selection.format_signals_message → push_message)
   ↓
[ユーザーがLINEで返信: "1 100" 等]
   ↓
⑤ Webhook受信 (line_webhook.py) → 署名検証 → parse_user_reply
   ↓
⑥ 発注待ちキュー書き出し (pending_orders/*.json) + reply返信
   ↓
⑦ 【未実装】楽天RSS発注実行(VBA or Python win32com)
   → pending_orders/ をポーリング → IFD/IFO・当日限りで発注 → 結果をprocessed/へ移動+ログ
```

## 実装済みコンポーネント(仕様確定・コードあり)

| ファイル | 役割 |
|---|---|
| `line_analysis_parser.py` | 分析テキスト(末尾JSON優先/regexフォールバック)→シグナル抽出。`to_order_dict()`で共通スキーマに変換 |
| `analysis_prompt_addendum.md` | 分析生成用Claudeプロンプトに追記するJSON出力指示 |
| `line_selection.py` | シグナルの保存・一覧テキスト整形・ユーザー返信(番号/コード+株数)の解析 |
| `line_webhook.py` | LINE Webhook。署名検証、follow時のuserId保存、返信解析→キュー書き出し+reply返信 |

### 共通データスキーマ(発注1件分)

```json
{
  "code": "6367",
  "side": "buy | sell",
  "entry_type": "limit | market",
  "entry_price": 25650.0,
  "take_profit": 24900.0,
  "stop_loss": 26200.0,
  "quantity": 100,
  "order_validity": "day_only",
  "order_style": "IFD | IFO",
  "confidence": "ok | needs_review",
  "ai_confidence": 0.75,
  "issues": [],
  "index": 1
}
```
- `ai_confidence`: Claudeが分析生成時に出す自信度・期待値(0.0〜1.0)。通知フィルタリング(下記)で使用
- `order_style`: take_profitとstop_lossが両方あればIFO、片方のみならIFD
- `order_validity`は現状"day_only"固定(仕様として当日限りのみを想定)
- `entry_price`はエントリー価格帯の中央値がデフォルト(必要なら発注実行側で上限/下限に変更可能)

### pending_orders/ キューの契約
- ファイル名: `orders_YYYYMMDD_HHMMSS.json`
- 中身: 上記スキーマの配列(ユーザーが返信で確定した分のみ、quantity付き)
- 発注実行側は処理後にファイルを`processed/`等へ移動し、二重発注を防ぐこと(★実装側の責務、未実装)

## 未実装コンポーネント①: 朝のpush送信フロー

**仕様:**
- トリガー: Windows Task SchedulerでPython/バッチを毎朝指定時刻(寄り付き前)に起動
- 処理順序:
  1. Claude APIへ分析生成プロンプト(JSON addendum込み)を投げる
  2. レスポンスを`line_analysis_parser.parse_message()`で解析
  3. `to_order_dict()`でスキーマ変換
  4. **フィルタリング**: 買いシグナルのみに絞り、期待値スコアで上位3〜5件を選定(詳細は下記「通知フィルタリングロジック」参照)
  5. `line_selection.save_today_signals()`で保存(通し番号付与、フィルタ後のリストに対して)
  6. `format_signals_message()`でテキスト生成
  7. 保存済みuserId(`line_user_id.txt`、followイベントで取得済み)宛にpush送信
- **未決定事項:**
  - Claude API呼び出し失敗時のリトライ回数・アラート方法(LINEに「本日は取得失敗」等を送るか)
  - 実行時刻(何時に送るか。寄り付き前で情報が古くならないタイミング)
  - 分析対象銘柄の選定ロジック自体(何を分析させるか)はこの仕様書のスコープ外(既存のClaudeプロンプト側の話)

**通知フィルタリングロジック(決定): 買いシグナルのみ・期待値上位3〜5件**

- **対象**: `side == "buy"` の銘柄のみを通知対象とする(売りシグナルは通知しない)
- **期待値スコアの算出**: リスクリワード比 × AI自信度 の組み合わせ
  ```
  risk_reward = (take_profit - entry_price) / (entry_price - stop_loss)
  expected_value_score = risk_reward * ai_confidence
  ```
  - `entry_price - stop_loss` が0以下(損切りラインがエントリー価格以上)の場合、そのシグナルは計算不能として除外する
  - `risk_reward` が極端に大きい/小さい外れ値になった場合の扱い(上限キャップをかけるか等)は要検討
- **選定件数**: `expected_value_score`の降順にソートし、上位5件を採用。ただし買いシグナル自体が3件未満しかない日は、ある分だけ通知する(無理に5件に満たす必要はない)
- **表示**: `format_signals_message()`に渡す前段でこのフィルタリングを行う。表示テキストにもスコア値を併記すると判断材料になる(例: 「期待値スコア: 2.3」)

**未決定事項:**
  - risk_rewardの外れ値(分母が極小でスコアが跳ね上がるケース等)への対処
  - 3〜5件という範囲の中で、常に5件を目指すか、日によって変動させてよいか

## 未実装コンポーネント②: 楽天証券RSSでのIFD/IFO発注実行

**前提:**
- Windows機でExcel + マーケットスピードII RSSが起動・ログイン済みであること
- RSS関数の正確な関数名・引数仕様は、楽天証券が提供する「RSS関数リファレンス」(RSS利用登録時にダウンロード可能なマニュアル)で必ず確認すること。ここでは一般に知られている概念のみ記載し、正確な関数シグネチャは断定しない。

**認証方式(決定): ID/パスワードによる自動ログイン**
- マーケットスピードIIのログインは「あんしんログイン」(生体/端末認証)ではなく、ID/パスワード方式を採用する
  - 理由: あんしんログインは生体認証が前提のため、無人での自動ログインができない
- ID/パスワードは環境変数、またはWindows資格情報マネージャー(Credential Manager)経由で保持し、スクリプトにハードコードしない
- **常時ログイン維持が必須**: マーケットスピードII/Excelは常時起動・ログイン状態を保つ運用とする(PCをスリープさせない、毎朝ログインし直す手間を無くす)
- **未決定の実装詳細:**
  - ログイン処理の自動化方法(マーケットスピードIIクライアントのログイン画面をUI操作で自動化するのか、別の手段があるのか)は要調査
  - セッション切れ・自動ログアウトを検知した場合のリトライ/再ログイン処理
  - 再ログインにも失敗した場合のフォールバック(LINEへ「本日は発注できません」等のアラートpush)

**仕様として決めるべき項目:**
1. **実行方式**: 以下いずれかを選択
   - (a) Excel VBAマクロが`Application.OnTime`で`pending_orders/`を定期ポーリング、JSONをVBA側でパースして注文発行
   - (b) Python側(win32com/xlwingsでExcelを直接操作)がポーリングしてRSS関数をセル経由で呼び出す
   - どちらでもキューの契約(JSON配列)は同じなので、実装言語だけの違い
2. **IFD/IFO発注時のマッピング**:
   - `entry_price` + `side` → 新規注文(指値 or 成行)
   - `take_profit`のみ → IFD(新規+利確)
   - `take_profit`+`stop_loss`両方 → IFO(新規+利確/損切のOCO)
   - `order_validity: day_only` → 執行条件を「本日中」に設定
3. **二重発注防止**: 処理済みファイルは`processed/`へ移動 or リネーム
4. **エラー時の扱い**: RSS接続断・発注拒否(証拠金不足等)の場合、ログに記録し、可能ならLINEへエラー通知をpush
5. **注文結果の記録**: 発注が通ったか、約定したかを追跡するログ(日時・銘柄・結果)

**未決定事項:**
- 上記(a)(b)どちらの実行方式にするか
- 発注失敗時にLINEへ通知するか、ログのみに留めるか
- 単元株未満(`unit_warning`)が付いた注文をそのまま発注するか、拒否するか

## モバイル(LINEアプリ)側の設定・入力体験

**基本方針:**
現状の設計(テキスト返信 "1 100")は、特別な設定なしでスマホのLINEアプリからそのまま入力可能。以下は快適に使うための追加設定・拡張案。

**1. 通知設定(スマホ側)**
- Bot(公式アカウント)とのトークが個別にミュートされていないか確認し、通知ONにしておく
- 通知のプレビュー表示(ロック画面等でメッセージ内容を表示するか)は要検討:
  - 表示ON: 通知だけで銘柄・価格が見える(速い)が、画面を人に見られた際に取引内容が漏れる
  - 表示OFF: セキュリティは高いが、アプリを開かないと内容が見えない
  - どちらもLINEアプリの「通知」設定、またはOS側の通知プライバシー設定(iOS/Androidそれぞれ)で変更可能

**2. 入力の手間を減らす拡張案: Quick Reply(ボタン返信)**
- LINE Messaging APIのpushメッセージには`quickReply`を付与でき、通知から開いた際にテキスト入力なしでボタンをタップするだけで返信できる
- 想定フロー(2段階):
  1. Step1: 「1」「2」「3」...と銘柄選択のボタンを表示
  2. Step2: 選んだ銘柄に対して「100株」「200株」「300株」等のプリセットボタン+「その他(手入力)」を表示
- **注意:** この方式にする場合、Webhook側に「ユーザーが今どの銘柄を選択中か」を一時的に覚えておく状態管理(セッション)が必要になる。現状の`parse_user_reply()`は1回の返信で完結する設計のため、拡張が必要(★仕様追加箇所)
- Quick Replyは1メッセージあたり最大13個までのボタン制限があるため、銘柄数(最大5〜6程度を想定)+株数プリセットの組み合わせ次第では2段階に分ける設計が現実的

**Quick Reply方式の詳細フロー(案):**
1. 朝のpush送信時、シグナル一覧テキストと合わせて「1」「2」「3」…(銘柄数分)のQuick Replyボタンを表示
2. ユーザーが番号をタップ → その番号のテキストがそのままメッセージとして送信される → Webhookが受信
   - 数字のみの返信は「銘柄選択」の入力として扱う
   - 選択した銘柄番号を、ユーザーごとに一時保存する(例: `pending_selection/{userId}.json`に銘柄番号+選択時刻を記録)
3. Botはreplyで「100株」「200株」「300株」「500株」「その他(手入力)」のQuick Replyを返す
4. ユーザーがプリセットをタップ → 保存しておいた選択銘柄+株数で発注を確定 → `pending_orders/`へ書き出し→受付完了をreply
   - 「その他」を選んだ場合は「株数を入力してください」と案内し、次のテキスト入力を株数として扱う

**この方式のデメリット・追加で決めるべき点:**
- 状態管理が新規に必要(現状のテキスト一括入力方式には無い仕組み)
- **タイムアウト設計が必要**: 銘柄だけ選んで株数を選ばずに放置された場合、一定時間(目安5分)で選択を破棄し、古い選択が翌日以降に誤って使われないようにする
- 複数銘柄を選ぶ日は「選択→株数」のやりとりを銘柄の数だけ繰り返すことになり、現状のテキスト一括入力(改行で複数行まとめて送信)より手数が増える
- ボタン数上限(1メッセージ最大13個)に収まるよう、銘柄数・株数プリセット数を調整する必要がある

**結論:**
最低限は現状のテキスト返信のままでスマホから支障なく使える。外出先でも素早く安全に返信したい場合は、Quick Reply方式(2段階選択)への拡張を検討する。どちらを採用するかは実装フェーズで決定。


- Webhookサーバーの常時稼働方法(自宅PC常時起動 / 小さいVPS / ngrok等でトンネル)
  → RSS側もPC常時起動・常時ログイン運用に決まったため、同じPCで両方を常時稼働させる前提で統一できる
- Claude API呼び出しのタイミングと、その日の相場前提条件(祝日・休場日の扱い)
- 完全自動ではなくLINE返信必須の設計にしたため、「返信を忘れた日」の挙動(何もしない、で問題ないか)
