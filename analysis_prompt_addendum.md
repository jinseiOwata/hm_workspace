【出力フォーマットの追加指示】

上記の分析本文(人間が読む文章)はこれまで通り出力してください。
そのうえで、本文の一番最後に、以下のルールに厳密に従った ```json ブロックを1つだけ追加してください。
本文中の数値と矛盾しないようにしてください。

- name: 会社名(必須)
- entry_low / entry_high: エントリー価格帯の下限・上限(単一値しかない場合は同じ値を両方に入れる)
- take_profit: 最初の目標株価(直近の節目。例:5日線水準)を1つだけ
- take_profit_2: 2つ目の目標株価があれば入れる(無ければ null)
- stop_loss: 損切りライン(必ず1つ)
- side: 現時点では常に "buy"(このBotは買いシグナルのみを対象としている)
- code: 4桁の証券コードのみ("6367.T"ではなく"6367")
- ai_confidence: このシグナルに対するあなた自身の自信度・期待値を0.0〜1.0の数値で(1.0が最も期待値が高い)。RSIの極端さ、複数の根拠の一致度、値動きの明確さなどを総合して判断すること
- reason: この銘柄を選んだ理由を全角40〜60字程度の一文で簡潔に(本文の詳細な根拠を要約したもの)

```json
{
  "signals": [
    {
      "code": "6367",
      "name": "DAIKIN INDUSTRIES",
      "side": "buy",
      "entry_low": 25600,
      "entry_high": 25700,
      "take_profit": 26200,
      "take_profit_2": 26800,
      "stop_loss": 25000,
      "ai_confidence": 0.75,
      "reason": "RSIが32まで低下し売られすぎ水準、25日線での反発初動を捉える"
    }
  ]
}
```

signalsの配列には、本文で取り上げた銘柄すべてを、本文と同じ順番で入れてください。
JSON以外の説明や```json ```以外のコードブロックは、この追加ブロックの中に含めないでください。
